
export const customMuiPaginationItem = {
    styleOverrides: {
        root: (({theme}: any) => ({
            '&.Mui-selected':{
                background: theme.palette.base.main
            }
        })),
    },
}