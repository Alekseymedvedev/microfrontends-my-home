import {Theme} from "@mui/material";

export const customMuiFormControlLabel = {

    styleOverrides: {
        label: {
            fontSize: 14,
            fontWeight: 400,
            lineHeight: `16px`,
        }
    },
}